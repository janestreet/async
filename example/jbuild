(executables (
  (names (
    bin_prot_test
    bind_loop
    cat
    countdown
    dns
    dynamic_port_writer
    echo_server_with_typed_tcp
    file_tail
    finalizer
    hello
    jobs_speed_test
    key_value_store
    load_file
    log_test
    monitors
    priority
    server
    sexp_with_text
    signals
    sigpipe
    socket
    speed_test
    tcp_file_client
    thread_pool_not_stuck
    timeouts
    write_forever))
  (libraries (async core))
  ))

(enforce_style ())
